import React from "react";
import classes from "./Experience.module.scss";

const Experience = (props) => {
  return (
    <div className={classes.experience}>
      <p>Placeholder Experience</p>
    </div>
  );
};

export default Experience;
